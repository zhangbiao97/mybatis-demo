<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.dao.EmployeeMapperPlus">

	<!-- 
		自定义javaBean的封装规则
			type：自定义类型
			id：唯一的标识，方便引用
	 -->
	<resultMap type="com.mybatis.entity.Employee" id="EmployeeResultMap">
		<!-- 
			指定主键列的封装规则
			column：指定哪一列
			property：指定对应的javaBean属性
		 -->
		<id column="id" property="id"/>
		<!-- 定义普通列的封装规则，不指定的列会自动封装 -->
		<result column="last_name" property="lastName"/>
		<result column="email" property="email"/>
		<result column="gender" property="gender"/>
	</resultMap>

	<!-- 
		注意：resultType和resultMap只能选择其一
	 -->
	<!-- Employee getEmpById(Integer id); -->
	<select id="getEmpById" resultMap="EmployeeResultMap">
		select * from employee
		where id = #{id}
	</select>
	
</mapper>